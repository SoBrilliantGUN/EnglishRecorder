# 任务00：项目结构规划

## 技术选型
- **框架**：React 19 + Vite 8 + TypeScript（strict 模式）
- **样式**：SCSS Modules + 语义化 CSS 变量 token 体系
- **外部依赖**：html2canvas（PNG 导出）、dayjs（日期处理）
- **数据存储**：localStorage

## 文件结构
```
EnglishRecorder/
├── README.md
├── CLAUDE.md
├── desc/                   # 项目描述文档
├── tasks/                  # 任务规范文档
├── scripts/                # 数据处理脚本
│   ├── split-sentences.mjs      # 逐句拆分 transcript（支持 --split-long 长句二次拆分）
│   ├── rebalance-transcripts.mjs    # 批量重拆分文字稿，长句按自然断点拆分至 ≤55 字符
│   ├── apply-translations.mjs   # podcast 翻译写入
│   └── apply-transcript-translations.mjs  # transcript 翻译写入
└── app/                    # Vite 项目根目录
    ├── tsconfig.json
    ├── public/
    │   └── transcripts/        # 课程数据唯一源（365 个，每个含 transcript + dialogue + 元数据）
    │       ├── englishpod_0001.json   # { code, title, level, transcript, dialogue }
    │       └── ...
    └── src/
    │   ├── main.tsx
    │   ├── App.tsx
    │   ├── App.module.scss     # Header + 图标按钮样式
    │   ├── index.scss          # 全局样式（引用 token 变量）
    │   ├── store.ts            # 数据层 + 工具函数（dayjs）
    │   ├── vite-env.d.ts       # 类型声明（SCSS modules 等）
    │   ├── data/
    │   │   └── podcasts-index.ts   # 课程元数据索引（id/code/title/level），构建时生成
    │   ├── types/
    │   │   └── podcast.ts      # PodcastMeta / Podcast / LEVEL_COLORS 类型定义
    │   ├── hooks/
    │   │   ├── useTheme.ts         # 暗黑模式 hook（ep_theme）
    │   │   ├── useCoef.ts          # 遗忘曲线系数 hook（ep_coef）
    │   │   ├── useLocalStorage.ts  # 通用 localStorage hook
    │   │   └── useScaleToFit.ts    # 小屏弹窗自动缩放 hook
    │   ├── styles/
    │   │   └── _variables.scss # 语义化颜色 token 体系（含 dark 模式）
    │   └── components/
    │       ├── icons/              # SVG 图标组件（单文件 .tsx，无样式）
    │       │   ├── index.ts        # 统一导出
    │       │   ├── ChevronLeftIcon.tsx
    │       │   ├── ChevronRightIcon.tsx
    │       │   ├── ChevronUpIcon.tsx     # 向上箭头，用于回到顶部按钮
    │       │   ├── CloseIcon.tsx
    │       │   ├── InfoIcon.tsx
    │       │   ├── MoonIcon.tsx
    │       │   ├── SearchIcon.tsx
    │       │   ├── SettingsIcon.tsx
    │       │   └── SunIcon.tsx
    │       ├── Modal/
    │       │   └── index.tsx       # 通用弹窗容器（overlay + 关闭逻辑）
│       ├── Pagination/
│       │   ├── index.tsx       # 通用分页组件（上一页/下一页 + 页码指示）
│       │   └── index.module.scss
    │       ├── Calendar/
    │       │   ├── index.tsx
    │       │   └── index.module.scss
    │       ├── DailyCard/
    │       │   ├── index.tsx
    │       │   └── index.module.scss
    │       ├── CheckinModal/
    │       │   ├── index.tsx
    │       │   └── index.module.scss
    │       ├── RecordsView/
    │       │   ├── index.tsx
    │       │   └── index.module.scss
    │       ├── PodcastList/
    │       │   ├── index.tsx
    │       │   └── index.module.scss
    │       ├── PodcastDetail/
    │       │   ├── index.tsx
    │       │   └── index.module.scss
    │       ├── ReviewReminder/
    │       │   ├── index.tsx
    │       │   └── index.module.scss
    │       ├── ReviewSettings/
    │       │   ├── index.tsx
    │       │   └── index.module.scss
    │       ├── InfoModal/
    │       │   ├── index.tsx
    │       │   └── index.module.scss
    │       ├── SettingsModal/
    │       │   ├── index.tsx
    │       │   └── index.module.scss
    │       └── ShareModal/
    │           ├── index.tsx
    │           ├── themes.ts
    │           ├── types.ts        # MultiCardProps / SingleCardProps / Theme 类型定义
    │           └── components/    # 卡片主题组件，单文件 .tsx，样式全内联（保证 html2canvas 正确）
    │               ├── CardDark.tsx
    │               ├── CardWarm.tsx
    │               ├── SingleCardDark.tsx
    │               └── SingleCardWarm.tsx
    └── package.json
```

## localStorage 数据结构
```typescript
{
  "ep_records": [
    {
      "id": "唯一ID（时间戳+随机数）",
      "date": "YYYY-MM-DD",
      "time": "HH:MM:SS",
      "lesson": 1,
      "count": 2,
      "timestamp": 1234567890
    }
  ],
  "ep_coef": 1.0,
  "ep_first": { "1": "2026-06-01" },
  "ep_resets": { "1": "2026-06-03" },
  "ep_initialized": true,
  "ep_theme": "light",
  "ep_show_review": "true",
  "ep_share_theme": "dark"
}
```

## 主题色与视觉规范
- **颜色 token 体系**：定义在 `src/styles/_variables.scss`，使用 CSS 变量（`--color-primary`、`--color-bg-card` 等）
- **主色**：`#07C160`（微信绿）
- **风格**：简洁大气，圆角卡片，柔和阴影
- **Toast 提示**：底部居中，CSS animation 控制显隐
- **按钮状态**：不可点击时置灰

## 响应式布局
- 桌面端：左右分栏（7:3，flex）
- 移动端：上下堆叠（flex-wrap）

## 开发注意事项
1. 不使用 setTimeout 等待异步操作
2. 不要在同一条消息中并发多个文件写操作
3. 用 `tick` prop 驱动子组件重渲染，避免在 useEffect 中直接 setState
